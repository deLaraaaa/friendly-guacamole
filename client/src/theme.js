import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1e88e5',
    },
    secondary: {
      main: '#43a047',
      dark: '#2e7031',
      light: '#68b36b',
    },
  }
});

export default theme;
